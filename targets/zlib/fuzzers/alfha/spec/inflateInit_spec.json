{
  "function_name": "inflateInit", 
  "function_code": "FC_ZLIB_INFLATEINIT",
  "function_location": "zlib.h:382",
  "category": "Compression",
  "priority": "Critical",
  "input_structure": {
    "strm": "z_streamp - pointer to z_stream structure"
  },
  "validation_conditions": [
    "strm != NULL"
  ],
  "error_codes": [
    "Z_OK",
    "Z_MEM_ERROR",
    "Z_VERSION_ERROR", 
    "Z_STREAM_ERROR"
  ],
  "side_effects": [
    "initializes internal stream state for decompression",
    "sets strm->zalloc/zfree if NULL",
    "initializes strm->total_in, total_out, adler, msg",
    "defers sliding window allocation to first inflate() call"
  ],
  "constraints": [
    "strm must be allocated before call",
    "next_in, avail_in, zalloc, zfree, opaque should be initialized by caller",
    "zalloc/zfree can be NULL (uses default)",
    "must call inflateEnd() after use"
  ],
  "fuzzing_strategy": {
    "input_generation": [
      "valid z_stream structure",
      "pre-initialized vs clean z_stream", 
      "NULL pointer testing",
      "custom vs default allocators",
      "various opaque values"
    ],
    "coverage_targets": [
      "memory allocation paths",
      "parameter validation",
      "default allocator assignment", 
      "state initialization",
      "version compatibility checks"
    ]
  }
}